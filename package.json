{
  "name": "siegestack",
  "version": "1.0.0",
  "private": true,
  "description": "siegestack.com. Static pages, plus Netlify functions for the contact form, the consultant expertise form, and the private threads at /jesse and /nicole.",
  "scripts": {
    "test": "node scripts/test/forms.test.mjs && node scripts/test/sweep.test.mjs",
    "test:defects": "node scripts/test/forms.test.mjs --defects && node scripts/test/sweep.test.mjs --defects"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.12",
    "nodemailer": "^9.0.5",
    "web-push": "^3.6.7"
  }
}
